server:
  port: 8080
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/fds
    username: postgres
    password: pass1234
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update # TODO none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
    format-sql: true